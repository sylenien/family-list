import React from 'react';
// import styled from 'styled-components';

// const Empty = styled.div`
//   align-self: center;
// `;

const ListEmpty = () => (
  <div>
    <h1>Oops! There is nothing here yet.</h1>
    <h3>Add new records via button below.</h3>
  </div>
)

export default ListEmpty;